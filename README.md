Install node - install it from https://nodejs.org/en/download/
Install npm - install it from https://nodejs.org/en/download/
Check node version - node -v
Check npm version - npm -v
Install cypress use - npm install cypress
Run project use - npx cypress run in comand line
run cypress open use - npx cypress open
